- name: Install tmux
  apt:
    name: tmux
    state: present

- name: Create symlink to tmux config
  file:
    src: /home/{{ lookup('env','USER') }}/git/dotfiles/tmux/tmux.conf
    dest: /home/{{ lookup('env','USER') }}/.tmux.conf
    owner: "{{ lookup('env','USER') }}"
    group: "{{ lookup('env','USER') }}"
    state: link

- name: Create tmux plugins directory
  file:
    path: /home/{{ lookup('env','USER') }}/.tmux/plugins
    owner: "{{ lookup('env','USER') }}"
    group: "{{ lookup('env','USER') }}"
    state: directory

- name: Clone tpm
  git:
    repo: https://github.com/tmux-plugins/tpm
    dest: /home/{{ lookup('env','USER') }}/.tmux/plugins/tpm
    update: no
  become_user: "{{ lookup('env','USER') }}"
  register: tpm

- name: Install TPM modules
  command: /home/{{ lookup('env','USER') }}/.tmux/plugins/tpm/bin/install_plugins
  become_user: "{{ lookup('env','USER') }}"
  when: tpm.changed
